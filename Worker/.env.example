WORKER_HOST=0.0.0.0
WORKER_PORT=8080
WORKER_LOG_LEVEL=info
WORKER_NAME=optimizer-1

# All benchmark data lives under Worker/datasets/
WORKER_DATA_DIR=datasets
WORKER_CHROMOSOMES=chr20,chr21

# Adaptive search algorithms: optuna, gp, random, sobol, lhs
# Trials after the mandatory base benchmark (total planned = 1 + this value).
# Sobol works best with powers of 2 (e.g. 32, 64). Main can override per job.
WORKER_ADAPTIVE_MAX_TRIALS=44

# Benchmark region inside dispatched window (Mb).
# 5 = full 5M round; 0 = entire dispatched window.
WORKER_BENCHMARK_SUBWINDOW_MB=5

# Per concurrent trial slot (Docker --cpus / --memory).
# DeepVariant needs >=16 GB RAM per trial; raise WORKER_TRIAL_MEMORY_GB when using deepvariant.
WORKER_TRIAL_THREADS=4
WORKER_TRIAL_MEMORY_GB=16

# DeepVariant GIAB benchmark overrides (optional):
# GIAB_DV_TIMEOUT=3600
# GIAB_DV_MEMORY_GB=16

# Layout (created by setup / first benchmark):
#   datasets/reference/{chr}/{chr}.fa + .fai + .sdf
#   datasets/giab/data/   — GIAB truth VCF + confident BED
#   datasets/giab/bam/    — regional HG002 BAM slices
#   datasets/giab/vcf/    — scored VCF reuse cache

# Path to minos_subnet checkout (templates + scoring). Default: parent of Worker/
# WORKER_REPO_ROOT=/path/to/minos_subnet

WORKER_PLATFORM_URL=https://api.theminos.ai
WORKER_REFERENCE_URL=https://api.theminos.ai/reference
