name: Tests & Status

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Unit Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('requirements.txt', 'requirements-dev.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run unit tests
        run: pytest tests/ --cov=utils --cov=templates --cov-report=term-missing -q

  status:
    name: Status Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run status check (JSON)
        run: python neurons/status.py --json --role miner > status.json || true
        # Exit code 1 is expected in CI (no Docker/refs) — we check critical fields below

      - name: Verify module imports pass
        run: |
          python -c "
          import json, sys
          report = json.load(open('status.json'))
          checks = {c['name']: c for c in report['checks']}

          # These must pass — code-level checks that catch regressions
          critical = ['Python version', 'Module imports', 'Python deps']
          failed = []
          for name in critical:
              if name in checks and checks[name]['status'] == 'fail':
                  failed.append(f\"{name}: {checks[name]['detail']}\")

          if failed:
              print('CRITICAL CHECKS FAILED:')
              for f in failed:
                  print(f'  ✗ {f}')
              sys.exit(1)
          else:
              print('All critical checks passed')
              for name in critical:
                  if name in checks:
                      print(f'  ✓ {name}: {checks[name][\"detail\"]}')
          "

      - name: Upload status report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: status-report
          path: status.json
